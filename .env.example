# Server-only Supabase PostgreSQL transaction-pooler URI.
# Copy this file to .env.local and replace every placeholder locally.
# Never prefix this variable with NEXT_PUBLIC_ and never commit a real secret.
DATABASE_URL="postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@YOUR_POOLER_HOST:6543/postgres?sslmode=require"
